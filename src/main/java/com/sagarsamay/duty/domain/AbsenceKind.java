package com.sagarsamay.duty.domain;

/** Why an ST is off. */
public enum AbsenceKind {

    /** Medical certificate. */
    MC,

    /** Off in lieu. */
    OL,

    /** Annual leave. */
    AL,

    /** Medical appointment — a couple of hours, not a day. */
    MA
}
